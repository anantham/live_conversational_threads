fastapi==0.115.12
python-multipart==0.0.20  # Required for FastAPI file uploads
python-dotenv==1.0.1
uvicorn==0.34.2
websockets==15.0.1
anthropic==0.52.1
# openai must be >=1.55.3 to work with httpx>=0.28 (older versions pass the
# removed `proxies` kwarg to httpx.Client — client CONSTRUCTION raises
# TypeError, breaking every OpenAI call path on a fresh install). Pinned to
# the version the code is developed and tested against locally.
openai==2.16.0
pydantic==2.11.5
google-genai==1.17.0
google-cloud-storage==3.1.0
# Used by share_api to verify Google Identity Services ID tokens on
# per-share email-allowlist enforcement. Verifies against Google's
# public certs over HTTPS; no client_secret config needed.
google-auth==2.34.0
databases[postgresql]==0.9.0
numpy==1.26.4

# Database migrations and ORM
alembic==1.13.1
sqlalchemy==2.0.25
greenlet>=3.0.0  # required by SQLAlchemy async (AsyncSession / create_async_engine)
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Instrumentation
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0

# Transcript parsing
pydub==0.25.1
pdfplumber==0.11.0
python-dateutil==2.8.2
httpx>=0.28.1

# Optional: VAD-based audio chunking (STT_VAD_ENABLED=true)
# Pulls in torch + onnxruntime (~500MB). Only needed if you enable VAD.
# pip install silero-vad

# Optional: Parakeet + separate pyannote diarization sidecar
# Only needed when STT_PARAKEET_PYANNOTE_ENABLED=true.
# pip install torch pyannote.audio
