# Meeting media deep links - test intent

- Raw-turn ingestion accepts bounded producer metadata while reserved privacy/contract fields remain server-authored.
- .threads exports include structured utterance rows and an allowlisted media_refs projection, never arbitrary source metadata.
- A static viewer builds a seek URL only for a valid supported media reference and finite timestamp.
- The evidence window remains usable without media, timestamps, speaker names, or a backend.
- A recording link is provenance, not embedded media; Google Drive permissions remain authoritative.
