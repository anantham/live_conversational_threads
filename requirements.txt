fastapi==0.115.12
uvicorn==0.34.2
websockets==15.0.1
anthropic==0.52.1
pydantic==2.11.5
google-genai==1.17.0
google-cloud-storage==3.1.0
databases[postgresql]==0.9.0

# Optional — only needed if ATTENDEE_SLOWPASS_ENABLED=true (services/attendee_audio_downloader.py).
# Not required for the rest of the backend; that module lazy-imports and skips cleanly if absent.
boto3==1.35.36